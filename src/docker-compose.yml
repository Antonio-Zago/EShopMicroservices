services:
   catalogdb:
     image: postgres:16

   basketdb:
     image: postgres:16

   catalog.api:
     image: ${DOCKER_REGISTRY-}catalogapi
     build:
       context: .
       dockerfile: Services/Catalog/Catalog.Api/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:

